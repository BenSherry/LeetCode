from typing import List


class Solution:
    def canPlaceFlowers(self, flowerbed: List[int], n: int) -> bool:
        count = len(flowerbed)
        if n <= 0:
            return True
        for i in range(count) :
            if ()
