#include <iostream>
#include <vector>
#include <string>

namespace diStringMatch
{
    class Solution
    {
    public:
        std::vector<int> diStringMatch(std::string S);
    };
} // namespace diStringMatch
